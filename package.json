{
  "scripts": {
    "flow": "flow",
    "lint": "eslint **/*.{js,jsx}",
    "lint-staged": "lint-staged --config .lintstagedrc",
    "pretty": "prettier --write **/*.{js,jsx,scss}"
  },
  "devDependencies": {
    "eslint": "^5.4.0",
    "husky": "1.0.0-rc.13",
    "lerna": "^3.1.4",
    "lint-staged": "^7.2.2",
    "prettier": "^1.14.2"
  },
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "wiki-map-people"
}
